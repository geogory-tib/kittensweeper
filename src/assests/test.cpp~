#include <raylib.h>




int main(void) {
  InitWindow(500, 500, "500");
  Texture2D test = LoadTexture("assests/deadyellowcat.png");
  Rectangle dest_rect {
    0, 0, 52.2,
        52.2
  };
  for (; !WindowShouldClose();) {
    BeginDrawing();
	DrawTexture(test,0,0,WHITE);
	EndDrawing();
  }
}
