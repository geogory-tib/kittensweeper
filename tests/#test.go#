package main

import (
	"math/rand"
)


func hash_func(x,y,length int) int{
	X2 := x + y + 4
	Y2 := x + y + 4
	hash :=((X2 + Y2)  % 81) % length
	return hash
}

func main(){
	for x := range 100{
		x := rand.Int % 9
		y := rand.Int % 9
		hash hash_func(x,y,71)
		
	}
}